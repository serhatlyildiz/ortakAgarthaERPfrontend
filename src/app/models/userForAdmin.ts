export interface UserForAdmin {
  email: string;
  firstName: string;
  lastName: string;
  city: string;
  district: string;
  adress: string;
  cinsiyet: string;
  status: boolean;
  roles: number[];
}
